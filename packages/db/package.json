{
  "name": "@repo/db",
  "version": "1.0.0",
  "description": "",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "postinstall": "prisma generate --schema=./prisma/schema.prisma",
    "dev": "pnpm db:generate && tsc -b && node ./dist/index.js",
    "build": "tsc --build",
    "db:generate": "prisma generate",
    "db:deploy": "prisma migrate deploy",
    "db:migrate": "prisma migrate dev"
  },
  "devDependencies": {
    "@repo/shared-secrets": "workspace:*",
    "@repo/typescript-config": "workspace:*"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module"
  
}
